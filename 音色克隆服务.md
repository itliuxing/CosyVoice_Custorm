# 音色克隆服务

## 一、功能流程

```
上传音频 → 系统检测可用性 → 输入文本 → 点击生成 → 下载音频
```

## 二、页面设计

### 2.1 上传音频
- 支持 .wav / .mp3 格式
- 建议时长 ≤30 秒
- 上传后显示：文件名、时长、采样率、是否可用

### 2.2 输入文本
- 单行文本框
- 提示用户输入要生成的文本内容

### 2.3 生成按钮
- 点击后调用后端接口
- 生成中显示 loading 状态

### 2.4 下载音频
- 生成成功后显示下载按钮
- 支持直接播放预览

## 三、后端接口

### POST /api/upload
上传参考音频，返回音频信息

**响应：**
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "filename": "xxx.wav",
    "duration": 5.2,
    "sample_rate": 16000,
    "text": "请输入音频对应的文本"  // 用户手动输入
  }
}
```

### POST /api/generate
执行音色克隆

| 参数 | 类型 | 说明 |
|------|------|------|
| audio_path | string | 音频文件路径 |
| prompt_text | string | 音频对应的文本（用户输入） |
| tts_text | string | 要生成的文本 |

**响应：**
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "output_path": "/output/xxx.wav"
  }
}
```

## 四、音频校验规则

| 检查项 | 要求 |
|--------|------|
| 格式 | .wav 或 .mp3 |
| 时长 | ≤30 秒 |
| 采样率 | 16kHz（自动转换） |
| 声道 | 单声道（自动转换） |

## 五、目录结构

```
project/
├── app.py
├── templates/
│   └── index.html
├── uploads/          # 上传的音频
├── output/           # 生成的音频
└── pretrained_models/
    └── Fun-CosyVoice3-0.5B/
```

## 六、待确认

1. prompt_text（音频对应文本）是否需要 ASR 自动识别？还是用户手动输入？